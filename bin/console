#!/usr/bin/env ruby
APP_ENV = ENV['RASPBERRY_MAILER_ENV'] || 'development'
PATH = ENV['RASPBERRY_MAILER_PATH'] || Dir.pwd

require 'pry'
require "#{PATH}/config/file_loader.rb"
Config::FileLoader.new(PATH).call

Pry.start

# TODO
# - use the same file for running app and console, but with options
